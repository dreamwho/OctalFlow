# 豆包助手

豆包和 Dola（国际版豆包）生成的图片及视频无水印素材提取工具，同时提供 ZIP 下载和豆包多账号切换功能。

> 本项目仅供学习和个人效率工具使用。账号数据只保存在本机浏览器，请自行妥善保管。

## v0.2.0 更新

- 修复 Dola 返回 HTTP 视频地址时，HTTPS 页面因 Mixed Content 限制无法直接下载的问题；下载前会自动将豆包和 Dola 媒体地址升级为 HTTPS。
- 新增批量下载，并为批量下载和 ZIP 打包增加全局 loading 与进度提示。
- 新增话题级全选，选择少于两项时禁用批量下载和打包下载。
- 优化单项下载提示、工作台启动按钮和扩展图标。

## v0.2.1 更新

- 修复扩展和油猴脚本未在豆包、Dola 的 `thread` 分享页加载，导致无法提取分享内容的问题。
- 分享页改为独立的“分享素材工作台”。
- 分享页展示标题、作者和分享时间，并增加分阶段加载提示与分类错误信息。
- 视频解析增加 12 秒超时和一次自动重试，失败后可通过刷新按钮再次解析。

---

## 功能特性

- **图片素材提取**：获取当前 Chat 对话中的图片素材和无水印原始地址。
- **视频素材提取**：解析生成视频的无水印下载地址。
- **轻量预览**：图片列表使用豆包返回的缩略图，视频列表只显示封面，不预加载原图或视频文件。
- **话题分组**：按照不同对话归类素材，并在每个话题内重新编号。
- **ZIP 打包下载**：将选中的图片或视频在浏览器端打包成 ZIP。
- **批量下载**：将选中的多个素材逐项下载，无需生成 ZIP。
- **链接操作**：支持复制素材地址和在新标签页打开。
- **豆包账号管理**：保存、添加和切换多个豆包账号。
- **Dola 支持**：Dola 支持素材提取、ZIP 下载和批量下载，但不支持账号切换。

## 支持范围

| 网站                  | 素材提取 | ZIP 下载 | 账号管理 |
| --------------------- | -------- | -------- | -------- |
| `doubao.com/chat/*`   | 支持     | 支持     | 支持     |
| `dola.com/chat/*`     | 支持     | 支持     | 不支持   |
| `doubao.com/thread/*` | 支持     | 支持     | 支持     |
| `dola.com/thread/*`   | 支持     | 支持     | 不支持   |

---

## 安装方法

### Chrome / Edge 浏览器扩展

1. 下载项目源码并解压。
2. 打开扩展管理页面：
   - Chrome：`chrome://extensions/`
   - Edge：`edge://extensions/`
3. 开启右上角的“开发者模式”。
4. 点击“加载已解压的扩展程序”。
5. 选择包含 `manifest.json` 的项目目录。
6. 打开豆包或 Dola Chat 页面开始使用。

扩展只会在豆包与 Dola 页面启用：

- 豆包页面：助手功能与账号管理均可用。
- Dola 页面：只启用助手功能。
- 其他页面：扩展图标自动禁用。

### Tampermonkey 油猴脚本

1. 安装 Tampermonkey 或兼容的用户脚本管理器。
2. 打开 Tampermonkey 管理面板。
3. 新建脚本或选择“导入文件”。
4. 导入项目根目录中的 `doubao-assistant.js`。
5. 保存后打开豆包或 Dola Chat 页面。

油猴版包含素材提取、ZIP 下载和批量下载，不包含账号管理。

---

## 使用说明

### 素材工作台

1. 打开豆包或 Dola 的 Chat 页面。
2. 点击页面右下角的“豆包助手”按钮。
3. 助手会在打开工作台时获取当前页面素材。
4. 使用“图片”和“视频”标签切换素材类型。
5. 可以下载单个素材、复制链接、在新标签页打开或选择多个素材打包下载。
6. 点击工作台外部区域可自动收起面板。

### 豆包账号管理

账号管理仅在 `doubao.com` 页面可用。

- **保存当前账号**：保存当前已登录豆包账号的 Cookie、名称和头像。
- **添加账号**：退出当前账号并打开新的登录窗口。当前账号尚未保存时，请先点击“保存当前账号”。
- **切换账号**：清理豆包站点数据，恢复目标账号 Cookie，并刷新豆包页面。
- **改名**：修改插件内显示的账号备注名称。
- **删除**：删除本机保存的账号快照。

---

### 账号切换

账号模块使用 Chrome 扩展 API 保存和恢复 `doubao.com` Cookie。账号身份优先通过稳定的登录 Cookie 匹配，Cookie 发生轮换时使用 `_ROUTER_DATA` 中的脱敏手机号或唯一账号名辅助识别。

---

## 文件说明

```text
manifest.json        Chrome Manifest V3 配置
doubao-assistant.js  助手唯一源码，同时也是油猴脚本
background.js        账号 Cookie、窗口与扩展状态管理
content.js           豆包账号资料与登录状态读取
popup.html           扩展弹窗结构
popup.css            扩展弹窗样式
popup.js             扩展弹窗交互
LICENSE              MIT 许可证
```

## 许可证

[MIT License](LICENSE)
