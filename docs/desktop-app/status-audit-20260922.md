# 桌面化计划实施与复测状态（2026-09-22）

核查依据：用户本次粘贴的三版本、阶段 0–8 计划；当前 `codex/desktop-app-foundation` 工作树；`docs/desktop-app/implementation.md`；本机测试与产物。**整体未完成，也没有完成计划要求的全功能复测。**

| 阶段 | 状态 | 已有证据与主要缺口 |
| --- | --- | --- |
| 0 现状冻结 | 部分 | `portability-and-boundaries.md` 已按当前协议注册表列出渠道可移植性、三版本数据归属、API/计费不变量与安全威胁；Web 关键页面/计费实际回归基线和各目标平台的 Provider 实测仍缺，不能把文档清单当成可移植性验收。 |
| 1 共享核心 | 未完成 | 根目录没有计划中的 `packages/` 共享核心；Canvas、创建工作台和 Provider 仍主要位于 `web/`。Web 与桌面复用的是打包后的 Web 运行时，尚未完成计划要求的共享包抽取及前后行为对照。 |
| 2 Electron 外壳 | 部分 | `apps/desktop/` 有 Main、Preload、单实例、loopback Runtime、打包脚本和两个版型；商用设备刷新凭据及两版 Runtime 安装密钥已由系统安全存储加密，本机烟测确认旧明文文件迁移后移除，仍未完成全部崩溃恢复、双版本全新电脑安装验收。 |
| 3 本地数据与媒体 | 部分 | 管理员版已有独立数据目录、文件型数据库、媒体登记、含素材 Canvas ZIP 导入导出和一次项目重启恢复实测；已增加密码加密的完整工作区备份/原子恢复与启动时按管理员周期自动备份，并在隔离的 macOS 打包应用中完成往返及不重复备份检查。跨电脑真机迁移、长时间不重启时的后台定时备份，以及计划中的 SQLite schema 和内容哈希引用计数闭环仍未完成。 |
| 4 本地 Provider Runtime | 部分 | 管理员版打包了 macOS arm64 的多个 Sidecar，并有配置页/Provider 启动烟测；全部渠道的本地模型、提交→状态→结果→日志完整矩阵，以及应用重启恢复和跨平台实际生成均未验证。 |
| 5 商用桌面版 | 部分 | 已有云端设备授权、令牌轮换、系统加密存储、本地用户绑定、仅限既有映射的离线项目恢复及受限的云端画布备份通道；离线退出会清除本地身份，将待撤销令牌加密保存并在恢复联网后补交。`implementationStage: foundation`、`cloudFeatures: false`、`cloudSync: false` 仍表示整体混合路由、实时积分、其他云端业务、自动同步和配置漫游未接入。设备登录与备份尚未在真实云端 PostgreSQL/OSS 环境联调。 |
| 6 用户云存储 | 开发中 | 工作树中新增默认 1 GiB、账户/权益/预留/对象表、用量/设置 API；原始字节流上传先预留额度，校验 SHA-256、写入 OSS 后重新读取校验并登记用量；重复文件共享对象，最后一个引用删除时才移除 OSS 文件。商品与订单已接入容量、手动续费、叠加、限购及支付/退款权益事务；用户管理编辑弹窗可读取用量。WEB 和商用桌面已有主动画布 ZIP 备份/下载/恢复通道，但仅有模拟数据库/OSS 与桌面 Main 传输测试；真实 PostgreSQL/OSS/支付、大文件和双机联调、跨设备素材自动同步、对象对账未完成。 |
| 7 管理员本地版 | 部分 | 免登录引导、部分云端路由 403、Canvas 与后台页面在本机烟测通过；本地备份页可加密备份与恢复完整工作区，启动时按设置周期自动备份，隔离打包应用中的项目和媒体往返成功。仍借用完整 Web 包，未做到构建时移除云模块；全部本地渠道、离线更新和完整断云验收未完成。 |
| 8 打包发布 | 未完成 | 只有 macOS arm64 管理员版未签名测试 DMG/ZIP；缺商用版及 macOS x64、Windows x64 共五类产物，缺正式签名、公证、更新回滚和干净机验收。 |

本次自动检查：`apps/desktop` 的 14 项测试通过；Web 全量复跑为 659 个测试文件、3222 项测试通过，6 个文件、12 项测试跳过；Web lint、类型检查和生产构建通过。重启生产服务后 `/api/health` 为 200，新云存储接口未登录访问为 401；Chromium 在 1440px 与 390px 浏览首页均为 HTTP 200、无横向溢出。商用桌面开发壳已用真实 Electron/Next Runtime 启动到设备授权页并截图，未配置云端地址时呈现明确阻断，退出后无 Sidecar 残留；云端网页入口在 Web 文件型开发库上按预期拒绝授权，不能当作云端联调通过。PostgreSQL 实库连接环境未配置，阶段 5、6 集成用例被测试开关跳过。上述检查不能替代计划中的云端联调、真实 Provider 生成、OSS 对账、双机同步、Windows/macOS x64 包和全新电脑安装验收。

交付门槛仍以原计划最终验收矩阵为准；当前分支和未签名包都不应标记为三个版本全部完成。

本轮补充验证：商用离线入口只接受 Electron Main 的独立令牌及既有本地用户映射，云端凭据明确失效不进入离线回退；该行为通过路由/单元测试，但没有真实云端账号断网端到端证据。两个 Electron 开发壳在本机重跑均启动成功，管理员本地版仍只显示本地后台分区；Web standalone 重新构建后扫描不到 `.data` 目录，生产健康接口为 200，普通客户端调用离线引导接口返回 403。

离线退出专项回归：模拟授权后断网退出，确认本机刷新凭据立即删除、待撤销令牌只以系统安全存储密文落盘；重新联网启动后向云端补交撤销并删除待办文件。该测试不代表真实云端撤销接口和多设备端到端验收完成。

工作树同时包含独立的 Dola Provider 与后台验证路由改动；本轮已对当前 Web 工作树重新执行全量测试、Lint、类型检查及构建，但没有调用真实 Dola 上游，不能把质量门禁当作该协议的业务验收。并行改动未被本次桌面开发修改或回滚。

云存储引用与删除专项：定向测试覆盖重复对象新增引用、共享对象保留、最后引用删除、OSS 删除失败时容量不释放和跨用户拒绝；PostgreSQL 实库案例已补充，但当前环境仍按测试开关跳过。

本轮当前工作树质量门禁：Web 659 个测试文件、3222 项测试通过（6 文件、12 项因环境跳过），Lint、TypeScript 和 74 页生产构建通过；生产服务已重启，健康接口 200，未登录 DELETE 云存储引用返回 401；Chromium 1440px/390px 首页均 200 且无横向溢出。Docs 首次构建暴露历史 MDX 裸 `<1440` 语法错误，改为代码片段后 38 页构建通过。这些回归不替代真实云端存储与桌面模型路由验收。

云存储下载响应现只返回引用、大小、摘要、来源和临时签名地址，不公开内部 OSS Key；新路由测试覆盖未登录删除 401、用户身份传入和下载响应脱敏。最终生产构建/重启后健康接口 200，未登录 DELETE 仍为 401，standalone 无 `.data`。

用户管理云存储统计：新增只允许 `users.manage` 或 `system.manage` 管理员读取指定用户用量的接口，弹窗打开时按目标用户加载并展示额度、预留及来源；路由权限测试通过。当前本地文件型 Web 环境没有云存储 PostgreSQL 账户，不能作为真实统计与浏览器往返验收。

当前工作树新增用户云存储统计后的质量门禁：Web 659 个测试文件、3222 项测试通过（6 文件、12 项跳过），Lint、TypeScript、74 页生产构建和 Docs 38 页构建通过；生产服务重启后健康接口 200，未登录用户统计接口 401，Chromium 1440px/390px 首页 200 且无横向溢出。用户管理弹窗在真实 PostgreSQL 数据下的浏览器验收仍未完成。

云存储套餐开发进展：计费商品新增独立 `storage` 类型，商品容量写入订单快照，支付成功按订单唯一授予有期限的容量，退款撤销权益；商品、结算和后台订单界面显示容量。单元测试覆盖商品校验、重复支付回调不重复授权及退款撤销。真实支付回调、PostgreSQL 权益生效、OSS 配额、套餐到期与两台设备并发的端到端验证仍未完成；项目备份、同步与对账仍是阶段 6 缺口。

商品规则补齐：管理员可设置同款容量是否叠加、是否允许手动续购、每人最多购买份数；商品行锁使并发下单的限购判断串行，订单快照锁定规则。不可叠加的续购从同款未撤销权益最晚到期时间开始，权益写入前锁定用户容量账户。定向测试覆盖限购、待支付订单、重复回调、顺延授权及 SQL 字段映射；自动续订/自动扣款不属于当前实现，真实 PostgreSQL 和支付回调仍未验证。

本轮质量门禁：Web 全量 660 文件、3232 项通过，6 文件、12 项因环境跳过；Web Lint、TypeScript、74 页生产构建、Docs 38 页构建及桌面 14 项测试通过。生产服务已重启。匿名浏览器会从 `/profile?section=billing` 返回首页，因此本机只能确认生产入口和页面宽度，不能把模拟商品响应当作已登录商品卡片验收。当前环境没有 PostgreSQL 与支付商户回调，真实购买、限购并发和 OSS 配额仍须联调。

最新质量门禁：Web 全量 659 文件、3226 项通过，6 文件、12 项因环境跳过；Web Lint、TypeScript、74 页生产构建、Docs 38 页构建、桌面 14 项测试均通过。生产服务重启后健康接口 200；未启用 PostgreSQL 的本机商品接口返回 501，结算页在 1440px/390px 匿名状态均 200 且无横向溢出。此处仍不是存储商品真实购买与账务回归。

画布云备份初版进展：沿用现有包含媒体的画布 ZIP 格式，WEB 项目卡可主动上传备份，项目列表可按当前用户查看、下载校验、恢复为新项目及删除备份。备份引用与项目索引在同一数据库事务写入；OSS 对象按内容去重，按云存储容量计费。后续已增加商用桌面设备令牌通道，详见下文。路由鉴权、归属、元数据与模拟存储测试已覆盖；当前无 PostgreSQL/OSS 联调环境，未完成双机恢复、上传中断、超额、套餐到期和真实 OSS 对账验证。阶段 6 仍为开发中。

本次复核的质量门禁：Web 全量 667 个测试文件、3249 项通过，6 个文件、12 项因环境跳过；Web TypeScript、ESLint、74 页隔离生产构建、Docs 38 页构建、桌面 14 项测试和 `git diff --check` 均通过。隔离生产服务健康接口返回 200，匿名访问云备份列表返回 401。Chromium 在 1440px 和 390px 从画布入口进入安装向导，页面没有横向溢出；由于该隔离服务未完成安装，不能视作已登录 Canvas 云备份界面回归。真实 PostgreSQL、OSS、支付回调、双机同步、全部 Provider 生成、macOS x64 和 Windows 安装验收仍未执行。**因此三个版本的计划功能没有全部实现，也没有全部复测；当前结果只能证明部分代码质量与匿名入口行为。**

商用桌面云备份增量：画布备份入口现对已授权的商用桌面开放，ZIP 的上传、备份列表、下载与删除由 Electron Main 使用短时设备令牌请求同一套云存储 API；Renderer 不接收设备令牌。云端备份路由接受当前 WEB Cookie 或有效设备令牌，并分别查当前用户，失效设备令牌不回退到 Cookie。单元测试证明请求路由受限、用户身份分离和下载摘要校验；真实云端数据库、OSS、跨电脑恢复、并发和大文件传输均未验收，商用桌面的其他云功能及混合模型路由仍未完成。

补充用量显示和传输回归：商用桌面备份弹窗经 Main 读取服务端已用、总额和可用空间；WEB 仍沿用原 Cookie 接口。Web 全量 669 个测试文件、3255 项通过，6 文件、12 项因环境跳过；Lint、TypeScript、74 页隔离生产构建、Docs 38 页构建和桌面 15 项测试通过。隔离 `pnpm start` 健康接口 200，匿名与无效设备令牌访问用量/备份接口均 401；真实 Node HTTP loopback 测试完成设备令牌上传、列表和下载字节传输。浏览器 1440px/390px 从画布入口均进入安装向导且无横向溢出。此处仍未验证真实云端账户、PostgreSQL/OSS、已登录画布备份界面或两台设备同步；阶段 5、6 保持部分/开发中。

设备退出登录并发回归：Main 在退出过程中阻止新的身份查询，已发出的迟到身份响应不能重新写入已删除的加密凭据。桌面测试增至 16 项并通过；该回归仍不能替代真实云端设备撤销联调。

版本能力修正：`cloudProjectBackups` 单独标记手动画布云备份，商用版为真、管理员本地版为假；整体 `cloudFeatures` 与 `cloudSync` 仍为假。画布入口现在读取该能力清单，不再根据版型名称自行推断功能已开放。

能力清单隔离烟测：商用环境的隔离生产服务 `/api/desktop/runtime` 返回 `cloudProjectBackups: true`、`cloudSync: false`；本地 `/api/cloud-storage/usage` 返回 403，生成路由仍返回 503，表明云存储只能走设备授权的云端通道，未完成的混合模型路由没有被意外开启。最终 Web 全量 669 文件、3255 项通过（6 文件、12 项环境跳过），桌面 16 项通过，Web/Docs 生产构建通过；此测试未连接真实云端账户、PostgreSQL 或 OSS。

管理员本地版优先验收：Web 与 Electron 现在读取同一份无密钥版本能力清单，Electron 安装包独立携带该清单。隔离 Web 生产构建成功；桌面打包支持读取指定的隔离构建目录，不触碰正在运行的 Web 构建。首次从新 macOS arm64 测试包启动时发现画布停在加载态，浏览器控制台证实 JS/CSS 404；已将静态资源复制到 standalone 实际服务目录并重包。该阶段的包在 `PATH=/usr/bin:/bin` 下启动后，自动烟测确认本地管理员身份、Canvas、Dola 管理页、渠道导航、计费接口 403，并新建项目、退出、重新启动后按 ID 读回再删除；桌面 19 项测试通过。该阶段尚未验证完整工作区备份与恢复，后续打包实测见下文。真实第三方账号的任务提交→结果、断网抓包、Windows/macOS x64、签名公证和干净机安装仍未验证；因此管理员版仍是“部分”，不能据此标记阶段 7 或 8 完成。

管理员桌面图标与零依赖进展：用户指定的 `全新UI设计/第一版/LOGO类/纯图标LOGO.png` 已按透明原图缩放为 `apps/desktop/assets/dreamyo.icns` 和 `dreamyo.ico`，macOS 安装包 `CFBundleIconFile` 指向前者且包内与生成文件的 SHA-256 一致；Windows 配置指向后者并完成 ICO 格式检查，未进行 Windows 原生打包。桌面 Mihomo 模板现在由 `apps/desktop/assets/` 提供，桌面构建不读取 `docker/`；Windows x64 冻结文件的模拟暂存测试也通过。macOS arm64 未签名 DMG/ZIP 实际在 `PATH=/nonexistent` 下启动，免登录画布、渠道后台、Dola 管理页和计费阻断烟测通过，证明该本机包启动链不需要 PATH 上的 Docker、Node 或 Python；尚未在完全干净的安装机器复测。Web 全量 670 文件、3262 测试通过，6 文件、12 测试环境跳过；Lint、类型检查、74 页隔离生产构建、Docs 38 页构建和桌面 19 测试通过。

长时间运行日志曾暴露冻结的 GeminiAIStudio 将 `multiprocessing.resource_tracker` 子进程参数误当成 CLI 子命令；已在冻结入口限定匹配并分派其标准资源追踪命令。重新冻结的 macOS arm64 Sidecar 健康检查及 fd=3 资源追踪子进程检查均成功；无账号时跳过浏览器预热，不再以匿名状态访问 Google 登录页。本机截图工具偶发 Chromium `UnknownVizError`，不带截图的相同安装包烟测连续通过；稳定页面截图改由 Electron 调试接口取得。管理员备份页已增加密码加密的完整工作区备份与恢复，macOS arm64 打包应用在隔离目录中实测：创建项目和媒体测试文件，备份后修改媒体，恢复后项目仍在、媒体回到旧内容、服务自动重启。自动备份实测在设置目录与密码后首次重启生成一份 `.dreamyo-workspace`，同日再次启动仍为一份，设置文件中未出现明文密码。桌面 22 项测试覆盖周期判定、错误密码、跨系统安全存储重加密和启动失败回滚；长时间运行中的后台备份与异机真机迁移仍待验证。

最终包校验：DMG 经 `hdiutil verify` 校验有效，ZIP 全部条目 CRC 验证通过；包内应用图标和 GeminiAIStudio 冻结程序各与其输入文件 SHA-256 相同。管理员版在 Electron 调试接口检查备份页的本地文案、目录桥接和实际点击返回成功，期间页面无外部域名请求。`git diff --check` 对本轮文件通过；全局检查仍报告并行修改的 `web/src/app/(user)/canvas/components/canvas-node-prompt-panel.test.ts` 文件末尾空行，此处未改动他人工作树。

管理员本地桌面 UI：项目页不再挂网站侧栏，管理员设置页以平面分类「模型与渠道 / 网络代理 / 运行记录 / 应用设置」和横向子页取代 Web 经营后台分组、折叠栏和广告卡，Web 后台分支保持原结构。macOS 测试包使用嵌入式原生标题栏；真实窗口截图证明红黄绿按钮与品牌、画布/设置应用操作位于同一深色顶栏。打包应用中由画布进入设置、切换代理分类、URL 与内容对应，1440px/1024px 无横向溢出；临时画布创建、编辑器标题区和设置入口检查后已删除测试项目。Web 全量 670 文件、3264 测试通过（6 文件、12 项环境跳过），桌面 19 项通过，Web 类型、Lint 和隔离生产构建通过。Windows 顶栏使用系统窗口按钮覆盖层的代码已加入，但没有 Windows 实机截图或安装验收；此处仅完成 UI 分层和 macOS 本机验证，不改变阶段 7/8 的未完成状态。

当前桌面 UI 配色及模型配置补充：管理员桌面项目库直接使用 Web 现有 `cosmic-night-bg.png` / `cosmic-light-bg.png` 月球背景，侧栏、设置分类、项目卡、Ant Design 主色与后台按钮采用 Web 的紫色系，浅深主题由 Electron Main 保存到本机应用目录。macOS arm64 包在 `PATH=/nonexistent` 下重新启动后验证主题、临时项目与设置均恢复；临时项目随后删除。当前这台电脑的 Web 配置通过桌面管理员接口导入独立数据库，重启后桌面返回 14 个渠道、42 个模型、4 类默认模型和 9 个重新加密的渠道密钥；从 Web 已标为可登录的账号中迁入 93 个 Dola Cookie，目标数据库的 93 个 Cookie 均为密文。最新 Web 全量 670 文件、3270 项测试通过（6 文件、12 项跳过），桌面 22 项通过，Web 类型检查、Lint 与生产构建通过；桌面 1440×960、1024×768 无文档级水平溢出。本轮没有执行真实上游的提交→生成→结果业务矩阵，也未验证新电脑凭据转移或 Windows 原生安装；这些项目仍保持未完成。macOS 本机身份签名的 `codesign --timestamp` 未完成，本次交付的是显式构建的未签名测试包，不能当作正式签名、公证发行版。

跨平台发行预检增量：桌面打包现读取每个 Provider、Mihomo、FFmpeg、即梦 CLI、深度推理程序与 Camoufox 的 Mach-O 或 PE 头，要求与目标平台和架构一致；macOS arm64 暂存的 10 个真实可执行文件已逐一通过，模拟 Windows x64 暂存测试确认 PE x64 被接受、PE arm64 被拒绝。实际管理员 macOS arm64 DMG/ZIP 已在新增预检下重建并通过校验；新包在隔离数据目录、`PATH=/nonexistent` 下通过免登录 Canvas、渠道设置、Dola 管理页与账务 403 烟测。桌面 22 项测试通过。此项不等于 Windows 原生冻结、安装或运行验收。

桌面视觉与启动复核：管理员项目库的深浅背景改为直接复用 Web `octal-ambient` 的月球图片、深色三层紫蓝青径向光和上下渐隐配方；桌面侧栏激活色、焦点线与主按钮继续沿用 Web 紫色体系。最终 macOS arm64 包经 Electron 调试接口截图核对，项目库的侧栏、紫色主按钮与月球背景均正常渲染；独立 Web 生产构建通过。打包 Runtime 现在等 Web、GeminiAIStudio、Dola API 和 GPTAPI 健康接口全部就绪才进入页面，避免启动初期把 Provider 未就绪误判为账号异常；正常烟测退出后未留下该安装包的 Sidecar。桌面 23 项测试通过。当前电脑的模型目录与凭据在独立应用数据目录，不写入安装包；全新电脑仍需通过加密工作区备份恢复或单独配置，不应把本机可用说成跨设备免配置。全部渠道的真实提交→结果、完全断网、Windows/macOS x64、正式签名公证和干净机安装验收仍未完成。

视觉证据：`screenshots/admin-library-moon-dark-20260922.png` 和 `screenshots/admin-library-moon-light-20260922.png` 来自最终打包应用的 1440×960 Electron 调试截图。浅色切换须等 Ant Design 控件主题一同更新后判定：稳定状态下搜索框白底深字、选中导航浅紫底紫字、主按钮紫底白字；切换的首帧不能作为最终主题截图。

使用当前电脑管理员应用数据的隔离副本进行一次不消耗生成额度的 Dola 登录态检查：账号列表 200、共 93 个账号；任选一个账号执行 `loginOnly` 后接口 200、`login_ready`、`loginProbe.state=ready`。这说明修复启动等待后，该包的一条真实账号登录检查链路可通；其他账号的状态、额度和实际视频生成仍需逐项验证。检查只写入隔离副本，原应用数据未改。
