# Dreamyo 桌面化实施记录与验收清单

更新：2026-09-23。基线为 `5c50e9d`，远端 `main` 和 `backup/pre-desktop-20260922` 标签；完整 Git bundle 位于项目同级 `Octal-Canvas-backups/`。开发分支为 `codex/desktop-app-foundation`。

## 三种运行形态

| 形态 | 身份与账务 | 业务数据 | 上游配置与生成 |
| --- | --- | --- | --- |
| Web | 云端登录、积分、支付 | 云端 PostgreSQL / OSS | 云端配置；全部按 Web 规则计费 |
| 商用桌面 | 云端实时登录、积分、支付 | Canvas/素材/短剧本地优先，购买容量后可备份 | 本地代理及五种 Provider 本地配置与执行不扣积分；其余模型走云端并实时扣费 |
| 管理员本地 | 无登录/积分/支付 | 仅本地，独立目录 | 所有渠道在本地配置，网络模型仍需自行提供上游凭据；本地调用不扣积分 |

Web 对外接口与后台仍保留全部功能。桌面版不能通过更改 URL、请求头或浏览器 JS 来改动版本：安装包内 `edition.json` 固定版型，进程仅绑定 `127.0.0.1`，随机运行令牌仅由 Electron 发给本地源。管理员本地版借用现有的权限、Session、文件存储逻辑，在独立数据目录自动建立本地管理员。此身份不得同步到商业云端。两个桌面版使用不同 appId/userData，Cookie、代理凭据、素材与项目不会随安装包分发。

## 已落地的第一阶段

- Electron 主进程、preload 白名单 IPC、沙箱隔离、单实例、防跨源导航、外部 HTTPS 链接分离、独立运行数据目录。
- 桌面 Runtime 的安装令牌、加密密钥和本地引导密码改由系统 `safeStorage` 加密后写入独立版本数据目录；既有明文 `runtime-secrets.json` 首次启动时在保留原密钥的前提下原子迁移并删除。系统安全存储不可用或密文损坏时拒绝启动，不自动生成替代密钥。本机两个版本均通过启动烟测，且只留下权限为 `0600` 的密文文件。
- Web standalone 的文件跟踪已排除运行期 `.data`，生产构建完成后再次清理并校验产物；本机最新构建扫描不到 `.data` 或 `.env*`。此前构建曾尝试复制瞬时 Dola 日志锁文件，此项修改消除了该跟踪风险。桌面打包仍保留独立白名单预检。
- 管理员免登录引导：桌面令牌 + loopback 双条件，生成普通 HttpOnly Session；Web 环境返回 403。
- 管理员本地版隐藏商业运营分区、账务入口、站点初始化向导和账号/邮件系统设置，并在代理层拒绝云端商业路由；本地生成计价为 0，仍保留任务/幂等流水契约。
- 管理员本地版的路由边界补齐了桌面云授权/撤销/设备、CDK、账户注销、邮件、签到、互动通知、远程版本检查、过期账单/退款/邀请结算和云端生成回调入口；本地 Provider 管理、Runtime、bootstrap 与 Canvas 仍可用。云路由根目录集中在 `web/src/lib/desktop-cloud-routes.json`，Web 代理和桌面打包共用此清单；管理员版打包还使用 `web/src/lib/desktop-admin-cloud-pages.json`。打包时会从独立 standalone 副本的 `app-paths-manifest.json` 移除这些云 API 与账号/商用页面入口，删除对应 handler 和页面 RSC 清单，并清理不再被保留页面引用的浏览器 JS/CSS chunk；商用包与 Web 构建保持完整。当前生产构建真实清单中移除 108 个云 API 与 18 个账号/商用页面入口，并从 159 个静态 chunk 中清理 71 个未被保留页面引用的 chunk。临时 standalone 副本启动实测健康接口和 Canvas 均为 200，云页面与账务 API 被拒绝，Dola 本地账号路由仍存在并正常进入鉴权（401），Canvas 首屏引用的 47 个 JS/CSS 资源全部返回 200。服务端 chunk 暂不清理：虽然静态路由 chunk 引用分析得到 685 个引用项且直接依赖闭包未发现遗漏，真实 standalone 在删除 606 个未被这些路由静态引用的服务端 chunk 后，健康 API、云 API、本地 Provider API 与 Canvas 均变为空响应；恢复这些 chunk 后全部恢复正常。这说明 Next/Turbopack 运行时还依赖入口静态引用分析未覆盖的服务端模块，完整服务端模块图隔离仍未完成，不能以路由拒绝代替此项验收。桌面运行的 Generation Worker 也不再启动 Web 专用退款补偿循环，保留本地生成与数据生命周期维护任务。
- 管理员版代理回归现在逐条读取 `desktop-cloud-routes.json`，覆盖每个云端根路径及子路径拒绝，并验证 Web 版同一路径行为仍由原路由处理；这证明清单内边界不回退，但不等同于完整的服务端模块图隔离或公网抓包验收。
- Next standalone、Worker 与三个 Provider 的桌面端口和账号目录隔离；桌面不执行固定端口残留进程清理。
- 打包白名单与平台 Sidecar 预检：未提供目标架构的冻结可执行文件和 Camoufox 浏览器时拒绝打包；预检还读取 macOS Mach-O / Windows PE 头，拒绝把异构 Provider、代理或媒体程序误装进目标包，防止发出需要 Python/Docker 或实际无法运行的假完整安装包。
- 即梦官方安装源的桌面暂存已新增 `pnpm stage:dreamina`：按目标平台取 macOS arm64/x64 或 Windows x64 文件，检查 Mach-O/PE 架构并记录供应商版本、目标文件名和 SHA-256；Windows 打包预检会再次核对官方 Windows x64 文件名和程序摘要。2026-09-23 从官方源下载的 Windows x64 CLI 报告版本 `1.4.18`，实测为 PE32+ x86-64，SHA-256 `13a817e455179ab994495eedb875cf845348d05f526b21c2ef207e1fc47f6014`；桌面暂存与打包预检相关测试通过。该文件只留在临时验证目录，本机不是 Windows，尚未运行 Windows CLI、冻结 Provider、生成 NSIS 安装包或做安装验收。
- 桌面 Mihomo 启动编排与独立私有订阅目录已接入，四种 Provider 代理出口和控制器只绑定 loopback；预检增加 Mihomo、FFmpeg/FFprobe、即梦 CLI、视频深度推理程序与固定模型文件。当前 macOS arm64 已准备并分别验证这些运行时文件，视频深度以本地固定模型完成离线推理，FFmpeg/FFprobe 以实际帧编码和探测验证，`prepare-runtime` 在本机通过；其他目标架构仍未构建。
- Dola Provider 已在当前 macOS arm64 机器上用 PyInstaller 冻结为独立程序，空账号健康检查正常且未授权的模型目录返回 401；Mihomo 官方 arm64 程序用私有生成配置实测可启动，控制器未授权 401、正确密钥可读版本。这不等于已通过实际账号的完整生成，也不等于 Windows 或干净机验证。

macOS arm64 管理员版已有仅供本机验收的未签名 DMG/ZIP；仍未完成干净目标机器验收和商业分发签名/公证，Windows 安装包以及商用桌面版的混合账务与跨设备同步也未完成。商用入口现为设备授权页：云端登录用户在网页确认一次性授权码，桌面主进程持有并加密保存刷新令牌；本地 Session 只在云端访问令牌验证成功后创建。这段链路目前只有单元与构建验证，尚未在真实云端 PostgreSQL 环境联调，也不代表桌面付费模型路由已接通。不要把 `runtime` 能力清单当成已实现云端同步的证据。

## 第二阶段：无依赖安装包

1. 对 macOS arm64、macOS x64 和 Windows x64 各自在目标系统构建 Next standalone 与 Sharp 原生库。不要跨架构复用构建结果。构建脚本只打包编译后的运行文件，严格排除 `.env*`、`.data`、Cookie、浏览器 Profile、备份、测试结果。
2. 将 Dola、GeminiAIStudio、GPTAPI 和独立 Gemini Camoufox launcher 分别冻结为可执行文件。`resources/sidecars/<platform>-<arch>/` 必须包含 `dola-api`、`geminiai`、`chatgpt-api`、`geminiai-browser`（Windows 加 `.exe`）及 `camoufox/` 完整浏览器目录、根目录 `version.json`，macOS 浏览器可执行文件旁另有同版本 `properties.json`。冻结时验证动态导入、模板/静态文件、证书、Playwright/Camoufox 包与浏览器匹配；输出 SHA-256 清单。生产进程只使用包内文件。
3. 对 GeminiTools OAuth 回调、即梦 CLI 固定版本、Mihomo 魔法/链式代理、FFmpeg/FFprobe 和视频深度推理建立同样的架构独立可执行文件及运行目录。包内缺少任何启用功能所需二进制时启动必须显示准确缺失项，不能静默回退系统 PATH。
4. 补齐签名与更新：macOS 签名/公证、Windows Authenticode/NSIS；签名更新清单、版本回退与本地数据兼容回归。首次安装和升级必须在一台没有 Node/Python/Docker 的干净虚拟机通过。

## 第三阶段：商用桌面云端身份与混合路由

- 云端提供单独的桌面设备授权（浏览器 OAuth/一次性授权码）。云端签发短时 access token 和设备绑定的 refresh token；本地系统钥匙串/Windows Credential Manager 保存刷新凭据。令牌不得进入 Canvas 项目文件、代理日志或备份。退出登录撤销设备令牌并删除本地凭据。
- 本地服务维护独立 `localUserId ↔ cloudUserId` 绑定；登录态必须由云端验证，不能将本地文件账号视为云端账号。多用户同机的数据目录按云端用户隔离。
- 云端模型请求直接提交云端 API，积分消费/幂等/退款由云端事务执行；本地代理、GeminiAIStudio、Dola、GeminiTools、GPTAPI、即梦 CLI 及其本地模型走本地 Provider，无积分消耗。模型目录合并时每条绑定带 `executionLocation: local|cloud`，任务创建时固定快照并落日志，禁止根据模型名猜测计费位置或运行中切换。
- Web 始终执行云端规则：即使同一用户在桌面配置过本地 Provider，浏览器访问网站仍用云端后台配置并消费积分。
- 云端调用需在线；离线时本地项目编辑与已就绪素材浏览可用，云端登录、支付、余额确认、云端模型调用显示明确离线状态，不假记扣费。重连后只重试有稳定幂等键且云端尚未受理的请求。

验收：同一个云端账号在 Web 调本地类别模型仍扣费，在桌面调用本机该类模型不扣费；桌面调云端模型实时扣费且生成记录关联原请求；断网不能绕过云端权益。管理员本地版不出现云端账号与付费入口，全部配置都只写入本机。

## 第四阶段：本地数据与可选同步

- 项目、Canvas 节点、创作记录、素材、短剧、生成任务与 Provider 账号写入本地持久库和本地媒体目录；桌面端使用与 Web 相同的任务状态机、引用计数与删除保护，断电重启后恢复轮询。平台公开作品、公告、公共 Skill/提示词可以缓存带 ETag 和更新时间的只读快照；后台配置数据按用户设定频率拉取，登录/积分/支付/模型调用始终实时。
- 本地配置同步默认关闭。用户显式启用时，本机对代理凭据、Cookie、Provider 密钥先用独立用户同步密钥加密，云端只存密文、版本、设备与更新时间；新设备解密需要用户授权，不把云端管理员后台密钥混入个人配置。字段冲突用 revision/ETag 和逐条选择，不做静默 last-write-wins；删除使用墓碑与保留期。
- 默认免费云存储 `1 GiB`，管理员可调整默认额度，超过默认额度必须购买单独存储套餐。云端 OSS 对象按 `tenant/userId` 隔离，上传申请和完成登记均验证身份、对象大小与配额；临时未完成分片按租约回收。用户管理展示已用、预留、可用、套餐有效期与按项目/素材/主动备份分解统计。项目备份与跨设备恢复属于同一额度；本地素材不上传就不计费。服务端以对象清单及去重存储键统计，不能相信客户端上报的字节数。
- 支付成功/退款/到期由云端变更存储权益；到期超额先停止新上传，给恢复/导出宽限期，不因套餐到期立即删除已存作品。同步频率只影响只读快照/配置，不影响购买与配额强校验。

验收：1 GiB 边界、多文件同时上传、取消/失败分片、重复备份、删除释放容量、套餐过期、两设备同一配置冲突、跨用户越权、加密密钥丢失与恢复均有服务端测试和干净机器端到端测试。

## 质量门禁和实际运行记录

- 每次 Web 改动执行 `cd web && pnpm test && pnpm lint && pnpm typecheck && pnpm build`，然后 `pnpm start` 重启生产运行时，回归 Web 原有登录、画布、后台与计费。
- Windows x64 桌面发行由 `.github/workflows/desktop-windows.yml` 在 Windows 原生 Runner 执行：按锁文件准备 Provider 环境、冻结运行组件、从官方发布源校验并暂存 Mihomo/Camoufox/媒体工具/即梦 CLI，再构建管理员与商用 NSIS 一键安装器；工作流会静默安装管理员包并运行现有 Electron 冒烟流程，随后上传两种安装包。工作流已写入仓库但尚未由 GitHub Windows Runner 执行，因此 Windows 安装包和全新 Windows 10/11 安装验收仍未完成；商用包还要求仓库变量 `DREAMYO_DESKTOP_CLOUD_ORIGIN` 指向 HTTPS 站点。
- 桌面执行 `cd apps/desktop && pnpm test`；管理员无账号首次启动、重启后项目持久化、Provider 数据/端口与 Web 隔离、双开拦截、非法外链和 Web 拒绝引导逐项验证。
- 商用版必须在云端联调环境验证登录、实时计费、云端生成、同步及 OSS；管理员版需验证无需云端服务可打开项目和本地配置。生成视频还需逐模型/比例/时长验证最终媒体而非只看任务受理。
- 当前阶段实测：管理员本地运行接口 `200`、免登录引导 `303`、健康接口 `200`；Electron 窗口的 Canvas 项目页与后台模型渠道页已通过 `capturePage` 截图复核，管理员页无用户与积分分区；实际创建一个画布项目，完全关闭并重启桌面进程后读到原项目，随后清理测试项目；商用入口无本地登录表单。打包预检按预期拒绝缺失的冻结 Provider 文件。以上不能替代无依赖安装包与商用联调验收。
- 打包版启动页必须在 Web 和内置 GeminiAIStudio、Dola API、GPTAPI 健康接口全部就绪后才打开；如果 Provider 未启动，保持启动状态并报告服务问题，不能把启动竞态写成账号登录失败。Electron 正常退出和自动烟测退出均等待 Web Runtime 停止，以回收 Worker 与内置 Provider。
- Web 首页在 `1440/390/430px` 的独立无账号浏览器回归中均没有页面横向溢出；现有 Web 生产运行时健康接口 `200`、桌面专用运行接口 `404`。完整 Playwright Canvas/Home 矩阵曾因复用已有 `.e2e-data` 使安装前置断言失败；改用隔离数据重跑时发现测试启动脚本仍预热真实 Gemini 账号，随即终止回归、恢复原测试数据，并单独重启 Web。测试夹具需先隔离所有上游账号及固定 Sidecar 端口，再安全完成全矩阵。
- Web 的 Dola 账号管理已加入隔离的 Camoufox 有头测试：新实例、新空白上下文只注入选定账号 Cookie；直连、任意已启用通用节点或 Dola 当前绑定的魔法/链式出口可用于本次测试，管理员可远程点击、输入、滚动、刷新画面和主动结束，也能回到仍在运行的会话。结束时明确选择“不保存”或“检测登录并保存”：只在 Dola 启动协议确认登录有效时，按原账号凭据版本原子回写浏览器最新 Cookie，版本已被其他操作更新则拒绝覆盖；协议结果不明确时再以浏览器真实启动结果核验，仍未确认则保留窗口与旧 Cookie。管理员须在关闭原生浏览器前从后台选择保存；直接关闭原生窗口可能使最新 Cookie 无法读取，后台对此有明确提示。该功能不改变已有 Web 生成路由和桌面版本的计费边界；桌面本地版使用同一 Provider 接口。
- 商用桌面设备登录基础链路已接入：云端提供一次性设备码、登录用户网页确认、一次性兑换、短时访问令牌和刷新令牌轮换/撤销；桌面 Main 进程使用系统 `safeStorage` 加密保存刷新令牌，访问令牌只留在进程内，并用云端 `/me` 验证后创建独立本地用户和 Session。商用壳已启动到授权页并截图复核；由于缺少真实云端 PostgreSQL 联调环境，端到端登录尚未验证。模型免费/付费分流未完成前，商用桌面生成接口返回明确 503，避免误按本地积分逻辑执行。
- 商用桌面离线恢复仅面向曾在线验证的本地用户：加密设备凭据包含最近一次验证的云端身份；网络失败或云端 5xx 时，Electron Main 用独立的临时主进程令牌请求本地恢复，服务端只查已有 `cloudUserId → localUserId` 映射并建立本地 Session。云端 401/403 不触发离线回退；本地生成仍按尚未完成的混合路由保护返回 503。当前为单元/路由验证，尚缺真实云端登录后断网端到端验收。
- 商用桌面退出账号在断网时先删除本机加密刷新凭据与 Session，把待撤销的设备令牌另行加密保存，重新联网时补交撤销；单元测试覆盖断网退出、重启后补交和待办清理。真实云端多设备撤销与断网登录恢复仍待联调。
- 商用桌面画布项目备份已接通受限的云存储通道，版本能力清单单列 `cloudProjectBackups`，不把它误写成整体 `cloudSync` 完成。Renderer 只把备份动作和 ZIP 字节传给 Electron Main，Main 以设备令牌访问云端用量和备份接口，云端按设备当前用户校验归属与容量；备份弹窗显示服务端用量，下载后仍按字节数及 SHA-256 校验再导入。本机单元测试覆盖受限路由、令牌不下发 Renderer、WEB Cookie 路径不变和损坏备份拒绝恢复；真实 PostgreSQL/OSS、跨设备项目恢复及大文件传输仍待验证，其他商用云业务仍关闭。
- 管理员本地版的 Web 和 Electron 共享 `web/src/lib/desktop-edition-manifest.json` 能力清单，打包时独立附在安装包资源中；`NEXT_DIST_DIR` 可指向隔离的生产构建目录，打包必须把其静态资源放在 standalone 服务读取的同名目录。macOS arm64 未签名测试包曾因 JS/CSS 被放到服务目录外而停在加载态，修正后新包通过免登录、本地项目重启恢复、渠道及 Dola 后台挂载、计费阻断烟测。完整本地工作区的一次手工加密备份与恢复也已在隔离的打包应用中实测；全部 Provider 生成、云模块构建时排除和跨平台验收仍待完成。
- macOS arm64 管理员版首次打包暴露空的 GPTAPI 工作目录被打包器省略，导致进程启动 `ENOENT`；现随包加入对应上游许可证和声明文件并回归。执行 `DREAMYO_DESKTOP_UNSIGNED_TEST=1 pnpm pack:admin:mac` 已产出本机测试用 DMG/ZIP，打包产物自身在 `PATH=/usr/bin:/bin` 下启动通过：管理员免登录 Canvas、账务路由 403、上游配置页及 Dola 账号管理页正常挂载，关闭后无残留 Provider 进程。本机的非正式签名尝试因签 Sharp 原生模块返回 `errSecInternalComponent`。现已补齐 Developer ID、Hardened Runtime、公证与钥匙串凭据的构建配置及前置检查；当前机器尚无 Developer ID Application 证书，故实际签名、公证提交和干净 Mac 安装验收仍待账号证书就绪后完成，未签名包不可作为商业交付。
- 管理员测试包的应用图标来自 `全新UI设计/第一版/LOGO类/纯图标LOGO.png`；安装包使用由原图生成的 `.icns`，Windows NSIS 配置使用 `.ico`。桌面代理模板位于 `apps/desktop/assets/`，启动和打包不调用 Docker。macOS arm64 包在 `PATH=/nonexistent` 下通过免登录、渠道、Dola 与账务隔离烟测；Windows x64 仅通过带假冻结文件的暂存契约测试，没有原生安装或运行验收。
- 管理员数据备份页对 JSON 业务记录和整个本地工作区做显式区分，提供打开数据目录入口、密码加密完整备份与恢复入口。Electron Main 在暂停本地服务后把项目、媒体、数据库、外观及解密后的本机凭据封装为加密备份；恢复前校验摘要和密码，在暂存目录解包，替换失败时回滚并重启服务。macOS arm64 隔离打包应用已完成项目和媒体测试文件的备份、修改、恢复，确认项目仍可访问、媒体回到备份版本、服务自动重启；单元测试覆盖跨设备安全存储重加密、错误密码、启动失败回滚。管理员还可选本地盘或已挂载 NAS 的备份目录、周期天数及密码；配置由本机系统加密存储保存，每次启动在本地服务运行前检查是否到期。隔离打包应用已实测设置后下次启动生成加密备份，再次同日启动未重复备份。应用长时间不重启时不会在后台触发定时备份；真实第二台电脑和 Windows 恢复仍待验收。冻结 GeminiAIStudio 入口额外处理 Python 资源追踪子进程，空账号时不预热登录浏览器；本机冻包检查通过，但尚未逐渠道真实生成内容并校验结果媒体。
