# 协议缺口、额度预算与补测交接

日期：2026-09-16。**最新环境矩阵见15/16：IPWO独立HTTP仍遇verify/slide；Camoufox直连取得ACK且无滑块；105行Cookie文件第1条账号本轮独立HTTP直连再次遇verify/slide；解析IPWO/us + Camoufox首次导航被`region-restricted`拦截；显式台湾IPWO节点 + Camoufox取得ACK且无滑块。计划v1.7固定Camoufox页面会话为正式Provider主传输，独立HTTP只作诊断/失败对照，代理产品能力统一复用通用代理管理，并要求Cookie导入同时支持单文件多账号和多文件单/多账号；首版应用已实现，最终媒体、目标Linux和真实 Provider 联测仍未完成。**

## 1. 当前可交付结论

| 能力 | 状态 | 证据/缺口 |
| --- | --- | --- |
| 两个视频模型 ID | 已确认 | `seedance_v2.5`、`seedance_v2.0`；action bar 配置与实际请求一致 |
| 原生文生视频 | 两格通过 | 2.5/5秒/16:9；Fast/10秒/9:16；下载、播放、FFprobe通过 |
| 六比例按模型联动 | 菜单与配置已确认；完整成片未通过 | 两模型都显示六档；没有12格成片证据 |
| 单图上传与请求附件 | 已通过 | prepare → ApplyImageUpload → CommitImageUpload → pre_handle → 10052附件块；960×540 PNG，commit URI与提交图片URI一致 |
| 普通单图生成 | 两模型原生媒体均通过 | Fast16:9为1280×720；2.5的3:4为720×960/5.041667秒，参考图效果通过；均非独立HTTP创建，见09/10 |
| 双图、特殊引用与首尾帧 | 未验证 | 不能把普通单图成功当作多图mention或首尾帧合同 |
| Fast / 15秒 | 未实测 | 用户明确目标上限15秒，5/10/15独立档位；不支持30秒作为本项目需求约束 |
| 2.5 / 15秒 | 额度阻塞 | A账号日次数上限；B账号15秒/21:9/一图返回需12、余4；没有15秒媒体 |
| 2.5 / 30秒 | 未提交 | 本轮额度/预算未覆盖；只属于2.5目标，不能推断支持或不支持 |
| URL 去水印 | 两个样本协议通过 | 2.5与Fast原片的起始/中间/结尾水印消失，帧数/时长不变，全文解码通过；见07 |
| 导入账号后反代 API | 首版已实现，真实闭环待验收 | Camoufox直连Case取得ACK且无滑块；应用已提供 Cookie 批量导入、账号调度、Provider 反代和 Canvas 参数；105文件第1条独立HTTP直连在上传后遇verify/slide；最终媒体、持久任务、目标Linux和正式API仍待T02/T14 |
| 滑块验证恢复 | 当前Camoufox Case未触发；真实挑战闭环未测 | 不能声称永久免验证；官方处理器和互斥恢复方案见13/14，作为异常恢复保留 |
| 精确额度与重置 | 未确认 | 只取得助手额度文案，没有验证独立数值端点或结构化价格/余额合同 |
| 多账号自动切换 | 未在本项目实现/未真实验收 | 参考账本测试通过不等于Dola上游切换通过；不得用换号掩盖已受理未知任务 |

## 2. 新账号预算：原两次加两次明确追加

[account-b-budget.json](evidence/live/account-b-budget.json)记录三次已授权提交：2.5/15秒/21:9/单图额度拒绝；Fast/5秒/16:9/单图成功；用户随后明确追加2.5/5秒/3:4/单图，请求已核实；等待中用户接受并停止跟踪；随后用户报告成功，已在原会话复核720×960/5.041667秒媒体。随后用户再次明确授权一次独立HTTP Fast5秒，第四次已提交但被要求滑块验证；此后用户切换新账号C并授权一次同参数HTTP测试，仍遇滑块；C单独记1/1，未自动重发，见14。

第一次请求实际为 `seedance_v2.5`、`duration=15`、`ratio=21:9`，块类型 `[10052,10000]`。上游文字回复：“本次视频生成需要消耗12个视频生成额度，今日剩余4个视频生成额度，无法生成该视频。请尝试降低配置后重试。”这是具体组合的费用提示，不证明所有15秒固定消耗12，也不能据此计算30秒一定消耗24；不能把“可测试两次”当成所有模型/时长都可以生成两次。

**B账号4/4、C账号1/1授权提交尝试已使用；两次HTTP请求扣量均未知。** Fast回复剩余3，本次2.5回复消耗2、剩余1；这些均为助手文案，没有独立余额/扣量端点，不可作为固定价格或可信余额。原两次额度范围由用户后续明确追加一次，不是自动放宽预算。

临时请求包装已移除。它只匹配该case，发送前检查模型、比例和至少一张真实图片块，改duration=15，并阻止同case重复发送。Fast5秒测试只做参数/图片校验和防重复，不修改原生body；随后刷新清除临时包装。以上操作均未修改项目业务源码。

## 3. 精确的未测矩阵

| 模型 | 1:1 | 3:4 | 4:3 | 9:16 | 16:9 | 21:9 |
| --- | --- | --- | --- | --- | --- | --- |
| 2.5 | 菜单确认 | **5秒单图3:4成片通过** | 菜单确认 | 菜单确认 | **5秒成片通过** | 请求15秒一图已确认；额度阻塞 |
| Fast | 菜单确认 | 菜单确认 | 菜单确认 | **10秒文生成片通过** | **5秒普通单图通过** | 菜单确认 |

2.5的10/15/30秒仍无成功成片；Fast5秒普通单图与10秒文生已成片；用户明确Fast最多15秒，目标档位5/10/15，15秒仍需实测；30秒不属于Fast范围。两模型比例档案必须独立配置，即使菜单完全相同。

## 4. 下次如何继续，避免浪费已有额度

1. 不再重做两条文生基线、两模型5秒单图或URL去水印样本；先看06/07/09/10和脱敏JSON。原样本可以从Dola历史按caseId恢复，不能以重新生成替代查询。
2. 用户提供具备所需额度的授权会话，并重新决定生成预算后，先读当前配置和真实额度/报价来源。若仍只有文字提示，保留unknown；不要发送多条“试试能否成功”的请求来估余额。
3. 按15直接实现**Cookie-only + Camoufox隔离反代**：先用无生成验证确认登录/能力，再从持久Provider发出下一条必要15/30秒测试，同时覆盖反代与能力，避免桌面成功后重复扣量。正式提交固定由Camoufox页面完成，默认直连；独立HTTP和IPWO+Camoufox都不能作为自动回退，后者当前还会在代理地区限制阶段阻断。出现真实挑战时再进入13/14的T02-V恢复流程。
4. Fast另验证15秒，不测30秒。2.5的15秒使用单图和21:9复现已捕获的完整请求；30秒使用2.5、两张自有PNG和3:4（仅在当前上游确实支持两图时）。如果成本不足，由用户决定额度或范围；不得静默改成Fast、10秒、拼接或视频变速。
5. 准备好上传资源、类型校验、附件顺序、请求摘要和持久case记录后，只发送一次创建。拿到ACK就记录原会话关联；超时、SSE结束、网页关闭后继续查询原任务，不换号新建。
6. 对结果记录真实模型请求、比例、时长、图片引用效果、完整播放、FFprobe、文件哈希与首中尾帧；上游文字、HTTP 200、SSE ACK各自单独列状态。
7. 用剩余经授权预算补两模型比例矩阵、普通引用/首尾帧角色支持边界；额度不足逐格列未测。故障/多账号并发/幂等优先用已脱敏fixture验证，不能消耗用户账号制造重复任务。
8. 计划已按用户最新的两模型5秒基线范围定稿；确认后执行T00—T15。上述剩余协议在对应实施任务中验证，完整需求验收时仍逐项检查；如上游证明不支持某档，报告具体证据，不用降档或视频处理冒充完成。

## 5. 交接文件与保密边界

- 16：105行Cookie单文件结构、第1条直协议失败、单文件多账号/多文件单账号统一导入合同和测试矩阵。
- 15：IPWO独立HTTP失败、Cookie+Camoufox无滑块ACK、主传输/Cookie导入/代理和任务调整。
- 14：新账号C独立HTTP复测、1/1预算、Canvas任务所有者受限验证弹层方案。
- 10/11：最新基线与用户验收范围、3:4请求参数、完整body模板和详细参数扩展方法。
- 09：Fast5秒图生成片、参数证据边界、可播放文件、参考图对比和Fast15秒上限需求。
- 06：协议请求、查询与图片块的真实结构及状态语义。
- 07：URL无水印提取已验证方法、源关联、解码格式、媒体证据与应用接入草案。
- `evidence/live/DOLA-B20F-005-I1.sanitized.json`、对应media JSON和MP4：Fast5秒图生闭环与实际规格。
- `evidence/live/DOLA-B25-015-I1.sanitized.json`：一图、15秒和额度拒绝白名单样本。
- `evidence/live/image-upload.sanitized.json`：上传初始化、签名协议名称、commit与附件关联；不包含STS值。
- `evidence/live/url-watermark-verification.json` 与两张 comparison：两个原片的候选版本和可见水印检查。
- 原始HAR/签名VOD只作为本机短期调研中间文件，现已在提取脱敏证据后清理，不提交Git。后续不能依赖这些短期签名作为产品凭据；必须取得当前授权会话并查询原结果。

本轮没有修改应用运行代码、数据库、账号配置或部署服务，未生成上传包、未提交Git。现有工作区的其他修改全部保留。
