# 双模型五秒图生基线与不同比例验证

日期：2026-09-16。Case：`DOLA-B25-005-I1-R34`。**2.5原生图生成片现已核验通过：单参考图、请求5秒、3:4；下载视频720×960、5.041667秒、121帧。创建来自此前原生浏览器操作，独立HTTP创建仍未通过。** HTTP实验见[12](12-独立HTTP协议实测.md)，官方滑块处理方案见[13](13-滑块验证处理与会话恢复.md)。

## 1. 授权和时间线

用户要求两个模型默认5秒带参考图，并使用与此前Fast16:9不同的比例，因此本次选2.5/5秒/3:4/单图。09:03:33.356 UTC（北京时间17:03:33）原生浏览器仅创建一次。09:15:29.640查询尚无媒体；随后用户接受本次请求测试并要求停止持续跟踪，当时按“请求核实，媒体未核验”交接。

用户后续报告该视频已经生成成功，本轮只回到**同一原会话**读取结果并核验原片，没有重新生成。已捕获09:41:19.016 UTC的原会话查询响应，含成功视频。历史等待记录保留在JSON的`historicalObservationAtUserStop`和`userAcceptance`，当前结论以`lastObservation`和媒体证据为准。

B账号累计四次明确授权提交尝试：15秒额度拒绝、Fast5秒原生成功、2.5本次原生成功、独立HTTP Fast5秒滑块阻塞。当前没有第五次创建。本轮下载/查询和检查公开JS均不新增生成额度消耗。

## 2. 完整创建请求证据

- 原生模型`Dreamina Seedance 2.5`，wire值`seedance_v2.5`。
- `chat_ability.ability_type=17`，`ability_param`为JSON字符串；解析后`duration=5`、`ratio="3:4"`、`camera_movement="fixed"`。
- 参考图`reference-boat.png`为960×540（16:9）。一条消息含10052图片附件，`upload_status=1`、`progress=100`；另一条消息含10000文字块，不强制并成同一个message。
- HAR保留完整POST body，补齐Fast样本创建body未导出的证据缺口。功能字段与完整脱敏模板已保存。
- 发送保护只核对模型、时长、比例、图片数量与上传完成状态，限制本case一次创建，不修改原生body；刷新已清除临时包装。
- SSE ACK匹配原会话。助手回复消耗2、剩余1、预计10分钟仅是当时文字，不是独立数值额度合同。

[请求证据](evidence/live/DOLA-B25-005-I1-R34.sanitized.json) · [完整body脱敏模板](evidence/live/DOLA-B25-005-I1-R34.body-template.sanitized.json) · [预算账本](evidence/live/account-b-budget.json)

模板的运行时身份必须由当前隔离会话/attempt产生，不可复用研究账号Cookie、图片URI和会话ID。

## 3. 成片和图像引用结果

| 检查 | 实际结果 |
| --- | --- |
| 原会话终态 | `/im/chain/single`成功；2074 creation block，creation.type=2，video.status=3；页面显示视频已生成 |
| 下载媒体 | 199,555字节；720×960，3:4；HEVC，24fps，121帧；视频/容器5.041667秒 |
| 音轨 | AAC、44.1kHz、双声道、4.960000秒；音轨长度与视频分别记录 |
| 全片解码 | FFmpeg exit0，无解码错误 |
| 浏览器播放 | videoWidth834、videoHeight1112，duration5.041667；readyState4，播放进度实际推进 |
| 播放与下载区别 | 页面选择了另一播放分辨率，834×1112与720×960均为3:4；不把缩略图384×512当下载规格 |
| 参考图核验 | 0/2.5/4.9秒画面保留红色船体和珊瑚色三角帆，主体位于竖幅中央，水纹/倒影有变化；原片保留Dola AI水印 |
| 比例设置 | 16:9参考图未覆盖用户明确的3:4；请求与实际媒体一致，且不同于此前Fast16:9 |

媒体SHA-256：`6f7ff7c779ae6c4ed668b5809771452c0e1d7f7b7613e88380da694a4bb83dca`。

[FFprobe/播放/视觉证据](evidence/live/DOLA-B25-005-I1-R34.media.sanitized.json) · [原生成视频](evidence/live/DOLA-B25-005-I1-R34.mp4) · [参考图与视频三时点对比](evidence/live/DOLA-B25-005-I1-R34-reference-comparison.jpg)

以上证明普通单图参考确实影响结果，不证明精确首尾帧、多图角色或任意素材均支持。

## 4. 必须分别记录的验证层次

| 验证层次 | 当前状态 |
| --- | --- |
| Fast原生5秒/16:9/单图 | PASS，1280×720，视频流5.041667秒；完整结果见09 |
| 2.5原生5秒/3:4/单图 | PASS，本页原片与完整请求证据 |
| 两模型原生文生基线 | PASS，2.5五秒16:9；Fast十秒9:16，见06 |
| 独立HTTP能力/查询/新图片上传 | PASS，见12 |
| 独立HTTP创建 | BLOCKED_VERIFICATION_REQUIRED；无SSE_ACK/生成任务ID；不能借用本页媒体标PASS |
| 官方滑块处理代码 | SOURCE已确认；真实弹窗/完成/恢复尚未验证，见13 |
| 导入账号后的隔离Provider/Linux闭环 | 未通过，T02/T02-V前置任务 |
| 两模型十秒图生、Fast15、2.5的15/30 | 分别待验收；5秒成功不能将整个矩阵自动标PASS |

用户要求先跑通5秒以确定基础协议，现已完成两模型的**原生网页**图生基线；HTTP与隔离服务仍需单独补齐。项目应用已按确认后的计划实施，目标部署与长时长能力仍需分层验收。

## 5. 15/30秒方法提取状态

已完成源码定位、功能拆解、当前协议映射和独立实现示例：[11-参数扩展实施细则.md](11-参数扩展实施细则.md)将解锁器可选时长扩展落到模型profile，将请求duration改写落到标准JSON编译器。2.5目标5/10/15/30，Fast目标5/10/15；保留model、ratio、附件与当前会话结构，不复用600字符正则。

“已提取”表示实现方法已进入详细计划，**不是已接入运行项目**。没有安装插件，没有15/30秒成功媒体，也没有以拼接/变速替代长时长。URL去水印已验证方法另见[07](07-URL去水印验证与接入方案.md)。

本轮文件清单见[13 §8](13-滑块验证处理与会话恢复.md#8-本轮交付文件清单)。仅更新文档和研究证据，未修改应用源码、数据库或运行服务。
