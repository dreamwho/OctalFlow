# 可直接交给 Luna 的执行指令

你在现有 dreamyo 项目中执行UI重建。用户已授权按本计划直接实施，不要重复询问。你不是创建新Demo，不重建技术栈，不只是改全局颜色。

工作区：`/Users/dream/Desktop/PythonProject/Octal-Canvas`，实际路径用`pwd -P`确认。任务入口：`docs/ui-rebuild-20260913/README.md`。

先完整读取AGENTS.md、README、01问题与证据、02设计规格、03任务卡、04验收手册、06配色参考与应用矩阵和配色令牌计划.json；加载用户要求的Product Design与VibeHub技能。用户已经选定B01按钮与C01全站配色，原设计板用于结构和品牌形态，不需要再次做三个方向让用户选择。evidence/01—07是上轮失败证据，本轮图1=B01（09文件）为生成按钮目标，本轮图2=C01（10文件）为全站配色目标，旧08已废弃。先前的“全站验收完成”已撤回，不能当当前通过证据。

执行规则：
1. 顺序T00→T25并遵守依赖，保留已有用户修改。T00先备份和区分当前修改，不能直接按origin/main全部打包。
2. 每卡开工读实际文件和调用链，更新任务状态.csv。按02的具体规格实施，不另选图标/按钮尺寸/配色方案。B01衍生资源制作完成前不要用magic.png代替。
3. 16–20px工具图标统一Lucide；Logo要验证实际合成无背景；浅深正文、光标、面板均独立；Canvas生成按钮按40px紧凑/44px标准及父容器规格；边框贴真实媒体外缘。
4. 每一工作批次完成相关测试、类型检查、生产build与start重启，然后真实浏览器操作。只检查DOM数量、HTTP200、源码包含字符串都不能代替视觉与行为验收。
5. 使用隔离测试数据、现有本地协议fixture，不调用用户真实付费渠道，不清空生产数据；不要泄漏密钥到日志、截图或报告。
6. 矩阵含34个页面入口和36个后台section，必须逐项核对；登录/安装要用正确身份；动态页通过fixture生成真实ID；重定向不算原页面通过。实施后新增/删除路由，同步表格。
7. 每张截图需实际保存，带route/theme/viewport/state/zoom和代码版本。旧用户图是before，不能标after。A01–A17逐项读，特别是textarea实际输入、按钮裁切链/点击命中、SVG内部描边而非根框、B01小尺寸清晰度。
8. 新ui-rebuild.spec.ts必须被playwright.config.ts的testMatch发现；执行--list验证，不能以“0 tests”当通过。390/430必须真实viewport。
9. 遇缺少外部服务时完成不依赖它的工作并把相关卡片BLOCKED，不能跳过后宣称全部通过。每次进展报告具体已查明/修复/待测，不报无法证明的百分比。
10. 最终按执行日创建中文部署包、逐字节校验资源闭包，列实际改动文件+一句说明、日志、before/after、未通过项。源码包和已构建产物分别说清。未经本次实际执行不得说Git/镜像/线上部署完成。

每卡完成回报格式：
- 任务ID和状态
- 实际修改文件 + 每文件一句改动
- 相关自动化命令、退出码和结果
- 浏览器URL、真实视口/主题/状态、截图和测量JSON
- 遗留/阻塞项、下一张可执行卡

持续执行已经授权的实施范围直到全部可完成工作完成；不要用新增全局!important、放大所有按钮、隐藏溢出内容或批量替换彩色图标应付问题。最终无证据项目必须明确列出。

V2.1特别要求：先按C01重建语义配色并落实到所有页面及状态；仅替换按钮图片不算完成。读取manifest防止误用旧08；B01白星/底板必须由最新源图派生，旧V2.0派生资源需要重新核对。
