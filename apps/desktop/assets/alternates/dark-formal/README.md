# 深色备用图标

`dreamyo-source.png` 是 2026-09-22 用户选定的第一张深色狗头图标原图，未修改图形或配色。`icon-sizes/`、`dreamyo.icns` 和 `dreamyo.ico` 均由该原图生成，分别供通用尺寸、macOS 和 Windows 使用。

当前桌面应用仍默认使用 `apps/desktop/assets/` 下的浅色图标。需要切换时，将本目录中的同名源图、尺寸文件、ICNS 和 ICO 复制到 `apps/desktop/assets/`，然后重新打包应用。
